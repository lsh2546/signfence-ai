# Server-only. Never expose these values through NEXT_PUBLIC_* variables.
FOXIT_CLOUD_API_HOST=https://na1.fusion.foxit.com
FOXIT_CLOUD_API_CLIENT_ID=
FOXIT_CLOUD_API_CLIENT_SECRET=

# Keep disabled unless an operator has separately approved one envelope send.
SIGNFENCE_ESIGN_SEND_ENABLED=false
SIGNFENCE_OPERATOR_MODE=false
